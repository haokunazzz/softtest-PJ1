# 测试用例设计文档

## 一、单元测试用例

---

## 1. UserServiceImpl

- 测试对象：`src/main/java/com/demo/service/impl/UserServiceImpl.java`  
- 测试函数：`src/test/java/com/demo/service/impl/UserServiceImplTest.java`

| 用例编号 | 用例描述 | 预期结果 | 测试结果 | 结论 |
|----------|----------|----------|----------|------|
| UT-USER-001 | findByUserID - 输入存在的用户ID "tester" | 返回对应的User对象 | 通过 | 正确 |
| UT-USER-002 | findById - 输入存在的主键ID=1 | 返回对应的User对象 | 通过 | 正确 |
| UT-USER-003 | findByUserID(Pageable) - 分页查询(page=0, size=10) | 返回isadmin=0的用户分页数据 | 通过 | 正确 |
| UT-USER-004 | checkLogin - 输入正确的用户名"tester"和密码"pwd" | 返回匹配的User对象 | 通过 | 正确 |
| UT-USER-005 | create - 输入新用户对象(userID="newUser") | 保存成功，返回用户总数 | 通过 | 正确 |
| UT-USER-006 | delByID - 输入存在的用户ID=5 | 成功删除 | 通过 | 正确 |
| UT-USER-007 | updateUser - 输入更新的用户对象(password="newPwd") | 成功更新 | 通过 | 正确 |
| UT-USER-008 | countUserID - 输入存在的用户ID "tester" | 返回计数值1 | 通过 | 正确 |

---

## 2. VenueServiceImpl

- 测试对象：`src/main/java/com/demo/service/impl/VenueServiceImpl.java`  
- 测试函数：`src/test/java/com/demo/service/impl/VenueServiceImplTest.java`

| 用例编号 | 用例描述 | 预期结果 | 测试结果 | 结论 |
|----------|----------|----------|----------|------|
| UT-VENUE-001 | findByVenueID - 输入存在的场馆ID=1 | 返回对应的Venue对象 | 通过 | 正确 |
| UT-VENUE-002 | findByVenueName - 输入"Badminton Hall" | 返回对应的Venue对象 | 通过 | 正确 |
| UT-VENUE-003 | findAll(Pageable) - 分页查询(page=0, size=5) | 返回场馆分页数据 | 通过 | 正确 |
| UT-VENUE-004 | findAll() - 查询所有场馆 | 返回所有场馆列表 | 通过 | 正确 |
| UT-VENUE-005 | create - 输入新场馆(name="New Venue", price=300) | 返回新场馆ID=10 | 通过 | 正确 |
| UT-VENUE-006 | update - 输入更新的场馆(price=350) | 成功更新 | 通过 | 正确 |
| UT-VENUE-007 | delById - 输入场馆ID=10 | 成功删除 | 通过 | 正确 |
| UT-VENUE-008 | countVenueName - 输入"New Venue" | 返回计数值1 | 通过 | 正确 |

---

## 3. OrderServiceImpl

- 测试对象：`src/main/java/com/demo/service/impl/OrderServiceImpl.java`  
- 测试函数：`src/test/java/com/demo/service/impl/OrderServiceImplTest.java`

| 用例编号 | 用例描述 | 预期结果 | 测试结果 | 结论 |
|----------|----------|----------|----------|------|
| UT-ORDER-001 | findById - 输入订单ID=1 | 返回对应的Order对象 | 通过 | 正确 |
| UT-ORDER-002 | findDateOrder - 查询时间范围内订单 | 返回时间范围内的订单列表 | 通过 | 正确 |
| UT-ORDER-003 | findUserOrder - 查询用户订单(page=0, size=5) | 返回用户订单分页数据 | 通过 | 正确 |
| UT-ORDER-004 | updateOrder - 更新订单(hours=4) | 订单状态=1，总价重新计算=600 | 通过 | 正确 |
| UT-ORDER-005 | submit - 提交新订单(hours=3) | 创建订单，状态=1，总价=600 | 通过 | 正确 |
| UT-ORDER-006 | delOrder - 删除订单ID=4 | 成功删除 | 通过 | 正确 |
| UT-ORDER-007 | confirmOrder - 确认存在的订单ID=5 | 订单状态更新为2(待处理) | 通过 | 正确 |
| UT-ORDER-008 | confirmOrder - 确认不存在的订单ID=5 | 抛出RuntimeException("订单不存在") | 通过 | 正确 |
| UT-ORDER-009 | finishOrder - 完成存在的订单ID=6 | 订单状态更新为3(已完成) | 通过 | 正确 |
| UT-ORDER-010 | finishOrder - 完成不存在的订单ID=6 | 抛出RuntimeException("订单不存在") | 通过 | 正确 |
| UT-ORDER-011 | rejectOrder - 拒绝存在的订单ID=7 | 订单状态更新为4(已拒绝) | 通过 | 正确 |
| UT-ORDER-012 | rejectOrder - 拒绝不存在的订单ID=7 | 抛出RuntimeException("订单不存在") | 通过 | 正确 |
| UT-ORDER-013 | findNoAuditOrder - 查询未审核订单 | 返回状态=1的订单分页数据 | 通过 | 正确 |
| UT-ORDER-014 | findAuditOrder - 查询已审核订单 | 返回状态=2或3的订单列表 | 通过 | 正确 |

---

## 4. NewsServiceImpl

- 测试对象：`src/main/java/com/demo/service/impl/NewsServiceImpl.java`  
- 测试函数：`src/test/java/com/demo/service/impl/NewsServiceImplTest.java`

| 用例编号 | 用例描述 | 预期结果 | 测试结果 | 结论 |
|----------|----------|----------|----------|------|
| UT-NEWS-001 | findAll(Pageable) - 分页查询(page=0, size=5) | 返回新闻分页数据 | 通过 | 正确 |
| UT-NEWS-002 | findById - 输入新闻ID=1 | 返回对应的News对象 | 通过 | 正确 |
| UT-NEWS-003 | create - 输入新新闻(title="new") | 返回新新闻ID=9 | 通过 | 正确 |
| UT-NEWS-004 | delById - 输入新闻ID=7 | 成功删除 | 通过 | 正确 |
| UT-NEWS-005 | update - 输入更新的新闻(title="update") | 成功更新 | 通过 | 正确 |

---

## 5. MessageServiceImpl

- 测试对象：`src/main/java/com/demo/service/impl/MessageServiceImpl.java`  
- 测试函数：`src/test/java/com/demo/service/impl/MessageServiceImplTest.java`

| 用例编号 | 用例描述 | 预期结果 | 测试结果 | 结论 |
|----------|----------|----------|----------|------|
| UT-MSG-001 | findById - 输入留言ID=1 | 返回对应的Message对象 | 通过 | 正确 |
| UT-MSG-002 | findByUser - 查询用户留言(page=0, size=5) | 返回用户留言分页数据 | 通过 | 正确 |
| UT-MSG-003 | create - 输入新留言(userID="tester") | 返回新留言ID=8 | 通过 | 正确 |
| UT-MSG-004 | delById - 输入留言ID=4 | 成功删除 | 通过 | 正确 |
| UT-MSG-005 | update - 输入更新的留言(state=1) | 成功更新 | 通过 | 正确 |
| UT-MSG-006 | confirmMessage - 通过存在的留言ID=5 | 留言状态更新为2(已通过) | 通过 | 正确 |
| UT-MSG-007 | confirmMessage - 通过不存在的留言ID=5 | 抛出RuntimeException("留言不存在") | 通过 | 正确 |
| UT-MSG-008 | rejectMessage - 拒绝存在的留言ID=6 | 留言状态更新为3(已拒绝) | 通过 | 正确 |
| UT-MSG-009 | rejectMessage - 拒绝不存在的留言ID=6 | 抛出RuntimeException("留言不存在") | 通过 | 正确 |
| UT-MSG-010 | findWaitState - 查询待审核留言 | 返回状态=1的留言分页数据 | 通过 | 正确 |
| UT-MSG-011 | findPassState - 查询已通过留言 | 返回状态=2的留言分页数据 | 通过 | 正确 |

---

## 6. OrderVoServiceImpl

- 测试对象：`src/main/java/com/demo/service/impl/OrderVoServiceImpl.java`  
- 测试函数：`src/test/java/com/demo/service/impl/OrderVoServiceImplTest.java`

| 用例编号 | 用例描述 | 预期结果 | 测试结果 | 结论 |
|----------|----------|----------|----------|------|
| UT-ORDERVO-001 | returnOrderVoByOrderID - 输入订单ID=1 | 返回包含场馆名称的OrderVo对象 | 通过 | 正确 |
| UT-ORDERVO-002 | returnVo - 输入订单列表(2个订单) | 返回对应的OrderVo列表 | 通过 | 正确 |

---

## 7. MessageVoServiceImpl

- 测试对象：`src/main/java/com/demo/service/impl/MessageVoServiceImpl.java`  
- 测试函数：`src/test/java/com/demo/service/impl/MessageVoServiceImplTest.java`

| 用例编号 | 用例描述 | 预期结果 | 测试结果 | 结论 |
|----------|----------|----------|----------|------|
| UT-MSGVO-001 | returnMessageVoByMessageID - 输入留言ID=1 | 返回包含用户名的MessageVo对象 | 通过 | 正确 |
| UT-MSGVO-002 | returnVo - 输入留言列表(2个留言) | 返回对应的MessageVo列表 | 通过 | 正确 |

## 二、集成测试用例



### 公共展示模块

| 测试对象 | src/main/java/com/demo/controller/IndexController.java<br>src/main/java/com/demo/controller/user/NewsController.java<br>src/main/java/com/demo/controller/user/VenueController.java |
| --- | --- |
| 测试脚本 | src/test/java/com/demo/controller/PublicContentControllerIntegrationTest.java |



| 功能点列表 | 用例编号 | 用例描述 | 测试结果 | 预期结果 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 首页聚合 | PC-01 | 访问 /index。 | 通过 | 首页返回成功并包含新闻、场馆、留言数据。 | 正确 |
| 后台首页 | PC-02 | 访问 /admin_index。 | 通过 | 返回 admin/admin_index 页面。 | 正确 |
| 新闻详情 | PC-03 | 使用有效 newsID 访问 /news。 | 通过 | 返回 news 页面并包含新闻对象。 | 正确 |
| 新闻分页 | PC-04 | page=1 调用 /news/getNewsList。 | 通过 | 返回第一页新闻数据。 | 正确 |
| 新闻分页缺省值 | PC-05 | 不传 page 调用 /news/getNewsList。 | 通过 | 按默认 page=1 返回第一页数据。 | 正确 |
| 新闻列表页 | PC-06 | 访问 /news_list。 | 通过 | 返回 news_list 页面并包含 total。 | 正确 |
| 场馆详情 | PC-07 | 使用有效 venueID 访问 /venue。 | 通过 | 返回 venue 页面并包含场馆对象。 | 正确 |
| 场馆分页 | PC-08 | page=1 调用 /venuelist/getVenueList。 | 通过 | 返回第一页场馆数据。 | 正确 |
| 场馆分页越界 | PC-09 | page=2 调用 /venuelist/getVenueList。 | 通过 | 返回空列表。 | 正确 |
| 场馆列表页 | PC-10 | 访问 /venue_list。 | 通过 | 返回 venue_list 页面并包含 total。 | 正确 |



### 前台用户模块

| 测试对象 | src/main/java/com/demo/controller/user/UserController.java |
| --- | --- |
| 测试脚本 | src/test/java/com/demo/controller/UserControllerIntegrationTest.java |



| 功能点列表 | 用例编号 | 用例描述 | 测试结果 | 预期结果 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 注册页 | UC-01 | 访问 /signup。 | 通过 | 返回 signup 页面。 | 正确 |
| 登录页 | UC-02 | 访问 /login。 | 通过 | 返回 login 页面。 | 正确 |
| 普通用户登录 | UC-03 | 普通用户正确登录。 | 通过 | 返回 /index，并写入 user 会话。 | 正确 |
| 管理员登录 | UC-04 | 管理员正确登录。 | 通过 | 返回 /admin_index，并写入 admin 会话。 | 正确 |
| 登录失败 | UC-05 | 输入错误密码登录。 | 通过 | 返回 false。 | 正确 |
| 用户注册 | UC-06 | 提交新用户注册信息。 | 通过 | 重定向到 login，并创建普通用户。 | 正确 |
| 普通用户退出 | UC-07 | 访问 /logout.do。 | 通过 | 重定向到 /index，并清空 user 会话。 | 正确 |
| 管理员退出 | UC-08 | 访问 /quit.do。 | 通过 | 重定向到 /index，并清空 admin 会话。 | 正确 |
| 修改个人信息 | UC-09 | 提交新用户名、新密码、邮箱、手机号。 | 通过 | 重定向到 user_info，并更新数据库记录。 | 正确 |
| 保留原密码 | UC-10 | passwordNew 为空时提交修改。 | 通过 | 其他信息更新，原密码不变。 | 正确 |
| 密码校验成功 | UC-11 | 使用正确密码调用 /checkPassword.do。 | 通过 | 返回 true。 | 正确 |
| 密码校验失败 | UC-12 | 使用错误密码调用 /checkPassword.do。 | 通过 | 返回 false。 | 正确 |
| 个人中心页 | UC-13 | 访问 /user_info。 | 通过 | 返回 user_info 页面。 | 正确 |



### 前台订单模块

| 测试对象 | src/main/java/com/demo/controller/user/OrderController.java |
| --- | --- |
| 测试脚本 | src/test/java/com/demo/controller/OrderControllerIntegrationTest.java |



| 功能点列表 | 用例编号 | 用例描述 | 测试结果 | 预期结果 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 订单管理登录校验 | OC-01 | 未登录访问 /order_manage。 | 通过 | 抛出 LoginException。 | 正确 |
| 订单管理页 | OC-02 | 登录后访问 /order_manage。 | 通过 | 返回 order_manage 页面并包含 total。 | 正确 |
| 预约页-带场馆 | OC-03 | 带 venueID 访问 /order_place.do。 | 通过 | 返回 order_place 页面并包含 venue。 | 正确 |
| 预约页-空白 | OC-04 | 访问 /order_place。 | 通过 | 返回 order_place 页面。 | 正确 |
| 订单列表 | OC-05 | 登录后调用 /getOrderList.do。 | 通过 | 仅返回当前用户订单。 | 正确 |
| 订单列表登录校验 | OC-06 | 未登录调用 /getOrderList.do。 | 通过 | 抛出 LoginException。 | 正确 |
| 新增预约 | OC-07 | 登录后提交 /addOrder.do。 | 通过 | 重定向到 order_manage，并新增待审核订单。 | 正确 |
| 新增预约登录校验 | OC-08 | 未登录提交 /addOrder.do。 | 通过 | 抛出 LoginException。 | 正确 |
| 完成订单 | OC-09 | 提交 /finishOrder.do。 | 通过 | 订单状态更新为 3。 | 正确 |
| 修改页 | OC-10 | 访问 /modifyOrder.do。 | 通过 | 返回 order_edit 页面并包含订单与场馆。 | 正确 |
| 修改预约 | OC-11 | 登录后提交 /modifyOrder。 | 通过 | 重定向到 order_manage，订单信息更新且状态重置。 | 正确 |
| 修改预约登录校验 | OC-12 | 未登录提交 /modifyOrder。 | 通过 | 抛出 LoginException。 | 正确 |
| 删除预约 | OC-13 | 提交 /delOrder.do。 | 通过 | 返回 true，并删除订单。 | 正确 |
| 按日期查订单 | OC-14 | 调用 /order/getOrderList.do。 | 通过 | 返回指定场馆指定日期订单。 | 正确 |



### 前台留言模块

| 测试对象 | src/main/java/com/demo/controller/user/MessageController.java |
| --- | --- |
| 测试脚本 | src/test/java/com/demo/controller/MessageControllerIntegrationTest.java |



| 功能点列表 | 用例编号 | 用例描述 | 测试结果 | 预期结果 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 留言页登录校验 | MC-01 | 未登录访问 /message_list。 | 通过 | 抛出 LoginException。 | 正确 |
| 留言页 | MC-02 | 登录后访问 /message_list。 | 通过 | 返回 message_list 页面并包含 total、user_total。 | 正确 |
| 公开留言列表 | MC-03 | 调用 /message/getMessageList。 | 通过 | 仅返回已审核留言。 | 正确 |
| 个人留言列表 | MC-04 | 登录后调用 /message/findUserList。 | 通过 | 返回当前用户全部留言。 | 正确 |
| 个人留言登录校验 | MC-05 | 未登录调用 /message/findUserList。 | 通过 | 抛出 LoginException。 | 正确 |
| 新增留言 | MC-06 | 提交 /sendMessage。 | 通过 | 重定向到 /message_list，并新增待审核留言。 | 正确 |
| 修改留言 | MC-07 | 提交 /modifyMessage.do。 | 通过 | 返回 true，内容更新且状态重置为 1。 | 正确 |
| 删除留言 | MC-08 | 提交 /delMessage.do。 | 通过 | 返回 true，并删除留言。 | 正确 |



### 后台用户管理模块

| 测试对象 | src/main/java/com/demo/controller/admin/AdminUserController.java |
| --- | --- |
| 测试脚本 | src/test/java/com/demo/controller/AdminUserControllerIntegrationTest.java |



| 功能点列表 | 用例编号 | 用例描述 | 测试结果 | 预期结果 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 用户管理页 | AUC-01 | 访问 /user_manage。 | 通过 | 返回 admin/user_manage 页面并包含 total。 | 正确 |
| 新增用户页 | AUC-02 | 访问 /user_add。 | 通过 | 返回 admin/user_add 页面。 | 正确 |
| 普通用户列表 | AUC-03 | 调用 /userList.do。 | 通过 | 仅返回普通用户。 | 正确 |
| 编辑用户页 | AUC-04 | 访问 /user_edit。 | 通过 | 返回 admin/user_edit 页面并包含 user。 | 正确 |
| 修改用户 | AUC-05 | 提交 /modifyUser.do。 | 通过 | 重定向到 user_manage，并更新用户信息。 | 正确 |
| 新增用户 | AUC-06 | 提交 /addUser.do。 | 通过 | 重定向到 user_manage，并创建用户。 | 正确 |
| 用户ID校验 | AUC-07 | 调用 /checkUserID.do。 | 通过 | 已存在返回 false，不存在返回 true。 | 正确 |
| 删除用户 | AUC-08 | 提交 /delUser.do。 | 通过 | 返回 true，并删除用户。 | 正确 |



### 后台场馆管理模块

| 测试对象 | src/main/java/com/demo/controller/admin/AdminVenueController.java |
| --- | --- |
| 测试脚本 | src/test/java/com/demo/controller/AdminVenueControllerIntegrationTest.java |



| 功能点列表 | 用例编号 | 用例描述 | 测试结果 | 预期结果 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 场馆管理页 | AVC-01 | 访问 /venue_manage。 | 通过 | 返回 admin/venue_manage 页面并包含 total。 | 正确 |
| 编辑场馆页 | AVC-02 | 访问 /venue_edit。 | 通过 | 返回 /admin/venue_edit 页面并包含 venue。 | 正确 |
| 新增场馆页 | AVC-03 | 访问 /venue_add。 | 通过 | 返回 /admin/venue_add 页面。 | 正确 |
| 场馆列表 | AVC-04 | 调用 /venueList.do。 | 通过 | 返回场馆列表。 | 正确 |
| 新增场馆-空图片 | AVC-05 | 空图片提交 /addVenue.do。 | 通过 | 重定向到 venue_manage，并成功创建场馆。 | 正确 |
| 修改场馆-不换图 | AVC-06 | 空图片提交 /modifyVenue.do。 | 通过 | 重定向到 venue_manage，并更新其他字段。 | 正确 |
| 删除场馆 | AVC-07 | 提交 /delVenue.do。 | 通过 | 返回 true，并删除场馆。 | 正确 |
| 场馆名校验 | AVC-08 | 调用 /checkVenueName.do。 | 通过 | 已存在返回 false，不存在返回 true。 | 正确 |



### 后台新闻管理模块

| 测试对象 | src/main/java/com/demo/controller/admin/AdminNewsController.java |
| --- | --- |
| 测试脚本 | src/test/java/com/demo/controller/AdminNewsControllerIntegrationTest.java |



| 功能点列表 | 用例编号 | 用例描述 | 测试结果 | 预期结果 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 新闻管理页 | ANC-01 | 访问 /news_manage。 | 通过 | 返回 admin/news_manage 页面并包含 total。 | 正确 |
| 新增新闻页 | ANC-02 | 访问 /news_add。 | 通过 | 返回 /admin/news_add 页面。 | 正确 |
| 编辑新闻页 | ANC-03 | 访问 /news_edit。 | 通过 | 返回 /admin/news_edit 页面并包含 news。 | 正确 |
| 新闻列表 | ANC-04 | 调用 /newsList.do。 | 通过 | 返回新闻列表。 | 正确 |
| 删除新闻 | ANC-05 | 提交 /delNews.do。 | 通过 | 返回 true，并删除新闻。 | 正确 |
| 修改新闻 | ANC-06 | 提交 /modifyNews.do。 | 通过 | 重定向到 news_manage，并更新新闻内容。 | 正确 |
| 新增新闻 | ANC-07 | 提交 /addNews.do。 | 通过 | 重定向到 news_manage，并创建新闻。 | 正确 |



### 后台订单审核模块

| 测试对象 | src/main/java/com/demo/controller/admin/AdminOrderController.java |
| --- | --- |
| 测试脚本 | src/test/java/com/demo/controller/AdminOrderMessageControllerIntegrationTest.java |



| 功能点列表 | 用例编号 | 用例描述 | 测试结果 | 预期结果 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 预约审核页 | AOC-01 | 访问 /reservation_manage。 | 通过 | 返回 admin/reservation_manage 页面并包含 order_list、total。 | 正确 |
| 待审核订单列表 | AOC-02 | 调用 /admin/getOrderList.do。 | 通过 | 仅返回待审核订单。 | 正确 |
| 通过订单 | AOC-03 | 提交 /passOrder.do。 | 通过 | 返回 true，订单状态改为 2。 | 正确 |
| 驳回订单 | AOC-04 | 提交 /rejectOrder.do。 | 通过 | 返回 true，订单状态改为 4。 | 正确 |



### 后台留言审核模块

| 测试对象 | src/main/java/com/demo/controller/admin/AdminMessageController.java |
| --- | --- |
| 测试脚本 | src/test/java/com/demo/controller/AdminOrderMessageControllerIntegrationTest.java |



| 功能点列表 | 用例编号 | 用例描述 | 测试结果 | 预期结果 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 留言审核页 | AMC-01 | 访问 /message_manage。 | 通过 | 返回 admin/message_manage 页面并包含 total。 | 正确 |
| 待审核留言列表 | AMC-02 | 调用 /messageList.do。 | 通过 | 仅返回待审核留言。 | 正确 |
| 通过留言 | AMC-03 | 提交 /passMessage.do。 | 通过 | 返回 true，留言状态改为 2。 | 正确 |
| 驳回留言 | AMC-04 | 提交 /rejectMessage.do。 | 通过 | 返回 true，留言状态改为 3。 | 正确 |
| 删除留言 | AMC-05 | 提交 /delMessage.do。 | 通过 | 返回 true，并删除留言。 | 正确 |
